// Array example
var example = [1, '3', null, {}]

// Put in this array elements "x" that satisfy the condition: become true if value is coerced to a boolean
var trueList = ['2', 45, 2, 1, 33, '0', true, '33', 3, '333', {}, 'undefined', [], [{}], -1]

// Put in this array elements "x" that satisfy the condition: become false if value is coerced to a boolean
var falseList = ['', 0, null, false, undefined]

// Put in this array elements "x" that satisfy the condition: x == "33"
var thirdList = [33, '33']

// Put in this array elements "x" that satisfy the condition: x === "false"
var fourthList = []
